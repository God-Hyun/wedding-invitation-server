name: Keep Render Alive

on:
  schedule:
    - cron: "*/14 * * * *"  # 14분마다 실행

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render server
        run: curl -s https://wedding-invitation-server-d373.onrender.com/api/guestbook?offset=0&limit=1 > /dev/null
